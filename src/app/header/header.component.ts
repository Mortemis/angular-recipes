import { Component } from '@angular/core';

@Component({
    selector: 'app-header',
    templateUrl: './header.component.html',
    styles: [`
        .pointer {
            cursor: pointer;
        }
    `]
})
export class HeaderComponent {
}